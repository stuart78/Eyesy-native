# Minimal Python dependencies for packaged Electron app
# (No gunicorn, eventlet — uses threading async mode only)
flask==3.1.2
flask-socketio==5.5.1
pillow==11.3.0
numpy==2.3.3
requests==2.32.3
simple-websocket==1.1.0
